# AI Agent Backend Configuration (Server-side)
# The backend proxies requests to the AI agent to avoid exposing API keys in the browser
AGENT_BACKEND_URL=http://ai-snow.reindeer-pinecone.ts.net:9292/v1/chat/completions
AGENT_BACKEND_API_KEY=your-api-key-here

# Database Configuration
POSTGRES_USER=appuser
POSTGRES_PASSWORD=apppassword
POSTGRES_DB=appdb
PGHOST=db
PGPORT=5433
DATABASE_URL=postgres://appuser:apppassword@db:5433/appdb

# Authentication (Keycloak)
KEYCLOAK_ISSUER=https://auth-dev.snowse.io/realms/DevRealm
KEYCLOAK_AUDIENCE=account
KEYCLOAK_CLIENT_ID=audrey-final
KEYCLOAK_JWKS_URI=https://auth-dev.snowse.io/realms/DevRealm/protocol/openid-connect/certs

# Environment
NODE_ENV=development
